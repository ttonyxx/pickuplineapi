<html>
    <head>

        <meta charset="utf-8">
		<meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
		<meta http-equiv="x-ua-compatible" content="ie=edge">
        <meta name="Keywords" content="Pick Up Lines,API,Pick Up Lines API,Pick Up">
        <meta name="Description" content="This is the best pick up line API that exists on the market right now. It's also free to use!">
        <meta name="google-site-verification" content="HqXMSS9aQVWeHB-z3mOcTAfZ2ZDbxcTXHV1mWmc31vI" />

        <link href="https://fonts.googleapis.com/css2?family=Chelsea+Market&display=swap" rel="stylesheet">
        <link href="https://fonts.googleapis.com/css2?family=Raleway&display=swap" rel="stylesheet">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/prism/1.5.0/themes/prism.min.css"/>

        <title>Pick Up Lines API</title>

        <style>
            body {
                font-family: 'Raleway', sans-serif;
            }

            p {
                font-size: 20px;
                margin: 3;
            }

            h1 {
                font-size: 50px;
            }

            .container {
                padding: 10px 20px;
            }
            
            footer {
                position: fixed;
                bottom: 0px;
                left: 0px;
                background-color: #F5F5F5;
                width: 100vw;
            }

            .large {
                font-size: 80px;
            }

            #gif {
                width: 80px;
            }

            #showHome:hover {
                cursor: pointer;
            }

            button {
                width: 180px;
                height: 50px;
                font-family: 'Raleway';
                font-size: 20px;
                border-radius: 3px;
                outline: none;
                box-shadow: none;
                transition: 0.3s all;
            }

            button:hover {
                cursor: pointer;
                border: none;
                box-shadow: 0 2px 2px rgba(0,0,0,0.3);
            }

            #documentation, #usage, #donate {
                display: none;
            }

            hr {
                background-color: #EEEEEE;
                height: 1px;
                border: none;
            }

            .link {
                text-decoration: none;
                line-height: 1;
                position: relative;
                z-index: 0;
                display: inline-block;
                padding: 5px 5px;
                overflow: hidden;
                color: #333;
                vertical-align: bottom;
                transition: color .3s ease-out;
                font-size: 30px;
            }

            .link::before {
                content: "";
                position: absolute;
                z-index: -1;
                top: 0;
                left: 0;
                transform: translateY(calc(100% - 2px));
                width: 100%;
                height: 100%;
                background-image: linear-gradient(60deg, #64b3f4 0%, #c2e59c 100%);
                transition: transform .25s ease-out;
            }

            .link:hover { 
                color: #fff; 
            }

            .link:hover::before {
                transform: translateY(0);
                transition: transform .25s ease-out;
            }

            .header {
                font-size: 30px;
                text-decoration: underline;
            }

            .ymargins {
                margin: 10px 0px;
            }

            .medium {
                font-size: 50px;
            }

            .medium-sm {
                font-size: 40px;
            }
        </style>
    </head>
    <body>
        <div class="container">
            <h1 style="margin-bottom: 0;">Welcome to the</h1>
            <h1 class="large" style="margin-top: 0;" id="showHome">Pick Up Lines API <img id="gif" src="https://media.giphy.com/media/xT1R9JrgVFXMizvGww/giphy.gif"></h1>

            <button id="showDocs" >Documentation</button>
            <button id="showUsage">Usage</button>
            <a href="https://github.com/TtonyxX/pickuplineapi"><button id="showGit">Github</button></a>
            <button id="showDonate">Donate</button>

            <br><br>
            <hr>
            <br>

            <div id="home">

                <p class="large">The best pick up line API out there</p>
                <p class="medium-sm"style="margin-top: 10px;">"This helped me pick up all the girls I met" - Unknown</p>
                <p class="medium-sm">"This helped me pick up all the boys I met" - Unknown</p>
                <p class="medium-sm" style="margin-bottom: 300px;">"I laughed so hard I got another ab. It might be a tumor though." - Unknown</p>

                <footer>
                    <div class="container">
                        <p>Have any suggestions for pick up lines? Send an owl <img style="width: 15px;" src="https://image.flaticon.com/icons/png/512/12/12426.png">!</p>
                        <p>-Brought to you by the quarantine gang</p>
                    </div>
                </footer>

            </div>

            <div id="documentation">
                <p><a class="link" href="http://<%= url %>/lines">https://<%= url %>/lines</a> to get all the pick up lines in our wonderful database</p>
                <p><a class="link" href="http://<%= url %>/lines/random">https://<%= url %>/lines/random</a> to get a random pick up line to brighten up your day</p>
                <p><a class="link" href="http://<%= url %>/lines/0">https://<%= url %>/lines/:id</a> to get a get a specific pick up line, replacing ":id" with the id of the pick up line</p>
                <br>
                <p>Have a great day and we wish you the best!</p>
            </div>

            <div id="usage">

                <p class="header ymargins">Use it in an api call</p>

                <p>If you're using it on the client side, the javascript might look something like:</p>

                <div>
                    <pre><code class="language-javascript">
                          fetch('https://<%= url %>/lines/random').then((response)=> {
                              return response.json()
                          }).then((result) => {
                              console.log(result)
                          })
                    </code>
                    </pre>
                </div>

                <br><br>

                <p class="header ymargins">Create awesome apps</p>

                <p>Whether it be a pick up helper, a random pickup line generator, or a cure to coronavirus, using this API will change the world for the better.</p>

                <br><br>

                <p class="header ymargins">Change the world</p>

                <p>The world is at your finger tips, use this power to innovate.</p>

                <br><br>
            </div>

            <div id="donate">
                <p class="medium">Donate <a class="link" style="font-size: 50px;" href="https://www.redcross.org/donate/coronavirus-donations.html/">here</a> to keep us going</p>
            </div>

        </div>

        <script src="https://cdnjs.cloudflare.com/ajax/libs/prism/1.5.0/prism.min.js"></script>
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
        <script>

            const pages = ['#documentation', '#home', '#usage', '#donate']
            const btnNames = ['#showDocs', '#showHome', '#showUsage', '#showDonate']
            var active = '#home'

            $('#home').children().css("display", "none")

            var delay = 0
            $('#home').children().each(function () {
                $(this).delay(delay).fadeIn(800)
                delay += 500
            });

            for(let i=0; i<pages.length; i++) {
                $(btnNames[i]).click(() => {
                    if(active != pages[i]) {
                        $(active).fadeOut(200, () => {
                            active = pages[i]
                            $(pages[i]).fadeIn(200)
                        })
                    }
                })
            }

        </script>

    </body>
</html>

